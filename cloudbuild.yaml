steps:
  - id: "Rodando docker-compose"
    name: 'gcr.io/$PROJECT_ID/docker-compose'
    args: ['docker-compose.cloudbuild.yaml', 'up', '-d']

steps:
  - id: "Rodando docker ps"
    name: 'gcr.io/cloud-builders/docker'
    args: ['ps']

# steps:
#   - id: "Rodando composer"
#     name: 'gcr.io/cloud-builders/docker'
#     args: ['exec', '-t', 'app', 'composer', 'install']